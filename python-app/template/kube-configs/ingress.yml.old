# kube-configs/ingress.yml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ${{values.name}}
  namespace: default
spec:
  ingressClassName: nginx
  rules:
  - host: app.local
    http:
      paths:
      - path: /app
        pathType: Prefix
        backend:
          service:
            name: ${{values.name}}
            port:
              number: 8080
  - host: localhost
    http:
      paths:
      - path: /app
        pathType: Prefix
        backend:
          service:
            name: ${{values.name}}
            port:
              number: 8080
