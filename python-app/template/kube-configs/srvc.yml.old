apiVersion: v1
kind: Service
metadata:
  name: ${{values.name}}
  namespace: default
  labels:
    app: ${{values.name}}
spec:
  selector:
    app: ${{values.name}}
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 5000
